[build-system]
requires = [
  "meson-python",
  "meson",
  "ninja",
  "numpy"
]
build-backend = "mesonpy"

[project]
name = "drs"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
  "numpy",
  "scipy",
  "matplotlib",
  "pyyaml",
  "soundfile"
]
